{{#each errors}}
  <div class="alert alert-danger alert-dismissible fade show" role="alert">
    {{text}}
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>
{{/each}}


<div class="col-md-4 mx-auto">
  <div class="card">
    <div class="card-header text-center">
      <h3>Editar Transaccion</h3>
    </div>
    <div class="card-body">
      <form action="/transaccion/edit-transaccion/{{transaccions._id}}?_method=PUT" method="POST">
        <input type="hidden" name="_method" value="PUT" />
        <div class="mb-3">
          <label for="tipo">Tipo</label>
          <select required class="form-control" name="tipo">
            <option value="ingreso">Ingreso</option>
            <option value="egreso" selected>Egreso</option>
          </select>
        </div>
        <div class="mb-3">
          <label for="description">Descripcion:</label>
          <textarea
            name="description"
            class="form-control"
          >{{transaccions.description}}</textarea>
        </div>
        <div class="mb-3">
          <label for="description">Monto:</label>
          <input required type="number"  name="monto" value="{{transaccions.monto}}" class="form-control"></input>
        </div>
        <button class="btn btn-primary w-100" type="submit">
          Guardar
        </button>
      </form>
    </div>
  </div>  
</div>

<button class="btn m-10 btn-success"> <a class="dropdown-item" href="/transaccion">Volver</a></button>